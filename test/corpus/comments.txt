===================
AL Comments Test
===================

// A single-line comment at the very top

/* 
    This is a block comment at the beginning of the file.
    (Note: if you require block comment support, you'll need to add a block_comment rule and update extras.)
 */

// Table declaration with inline comments
table 50102 "Test Table with Comments"  /* Block comment appended to the table header */ 
{
    // Fields section starts here
    fields
    {
        field(1; "Field1"; Integer) // Inline comment for Field1
        {
            // A comment inside the field block
            Caption = 'Test field caption';  /* Block comment inside field */
        }
        /* Block comment between field declarations */
        field(2; "Field2"; Decimal) // Inline comment for Field2
        {
            Caption = 'Another caption';
        }
    }
    
    /* Block comment after fields: End of table declaration */
}

// A final single-line comment at the end of the file

---

(source_file
  (table_declaration
    (object_id (integer))
    (object_name (quoted_identifier))
    (fields
      (field_declaration
        (integer)
        (quoted_identifier)
        (data_type (identifier))
        (code_block
          (caption_property (string_literal))))
      (field_declaration
        (integer)
        (quoted_identifier)
        (data_type (identifier))
        (code_block
          (caption_property (string_literal))))))
